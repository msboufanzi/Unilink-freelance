/** @type {import('next').NextConfig} */
const nextConfig = {
  // reactStrictMode: process.env.NODE_ENV !== "production",
  images: {
    domains: ["unilink-freelance.onrender.com"],
  },
};

module.exports = nextConfig;
